<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Blog Articles</h1>
        <a href="/articles/new"  class="btn btn-success">New Article</a>

        <% articles.forEach(article=>{ %>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title"> <%= article.Title %></h4>
                    <div class="card-subtitle text-muted mb-2"><%=article.Date.toLocaleDateString() %></div>
                    <div class="card-text mb-2"> <%=article.Description%></div>
                    <a href="/view-article/<%= encodeURIComponent(article.Title) %>" class="btn btn-primary btn-sm col-sm-1">Read More</a>
                    <a href="/edit-article/<%= encodeURIComponent(article.Title) %>" class="btn btn-secondary btn-sm col-sm-1">Edit</a>
                    <form action="/delete-article/<%= encodeURIComponent(article.Title) %>" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                    </form>
                </div>
            </div>
       <% }) %>
    </div>
</body>
</html>